<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color superior -->
    <item android:top="0dp" android:bottom="50dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/primary"/>  <!-- Color superior -->
        </shape>
    </item>

    <!-- Color inferior -->
    <item android:top="280dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/onPrimary"/>  <!-- Color inferior -->
        </shape>
    </item>

</layer-list>